defmodule BlauDrill do
  @moduledoc """
  BlauDrill — single-operator desktop control app for precision PCB drilling.

  This module is the root of the `BlauDrill` business-logic context. The web
  interface lives under `BlauDrillWeb`. The application is intentionally
  **ephemeral and stateless across sessions** — one board per session, no
  database. See `CLAUDE.md` for the product brief and safety model.
  """
end
