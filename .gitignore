# Gleam build artifacts.
build/
*.beam
*.ez
erl_crash.dump

# Gleam's static-build output (produced by `gleam run -m lustre/dev build`).
# Regenerated from source; not checked in.
dist/

# Temporary files.
/tmp/

# Nix / direnv dev environment.
.direnv/

# Local Claude Code settings (per-developer MCP approvals, etc.)
.claude/settings.local.json
